(function () {
  'use strict';

  angular
    .module('application.news', [
      'application.news.controllers',
      'application.news.services'
    ]);

    angular
        .module('application.news.controllers', []);

    angular
        .module('application.news.services', []);

})();
