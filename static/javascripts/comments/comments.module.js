(function () {
  'use strict';

  angular
    .module('application.comments', [
      'application.comments.controllers',
      'application.comments.services'
    ]);

    angular
        .module('application.comments.controllers', []);

    angular
        .module('application.comments.services', []);

})();
